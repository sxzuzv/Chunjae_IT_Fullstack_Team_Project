<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis.mapper.UserMapper">

    <!--신수진-->



    <!--최경락-->



    <!--최영주-->

    <select id="authenticateUser" parameterType="map">
        select *
        from user
        where user_id = #{user_id} AND user_pwd = #{user_pwd}
    </select>


    <!--최재혁-->
    <insert id="createUser" parameterType="dto.UserDTO">
        INSERT
        INTO user(user_id, user_nick, user_pwd, user_name, user_cp, user_email, user_addr, user_daddr, user_school)
        VALUES(#{userId}, #{user_nick}, #{userPwd}, #{userName}, #{userCp}, #{userEmail}, #{userAddr}, #{userDaddr}, #{userSchool})
    </insert>



</mapper>